# src/components/layout/

هيكل التطبيق (FOS-003): `AppShell` (Sidebar لسطح المكتب + Drawer للجوال + Topbar)، `AppNav` بالعناصر الستة من `nav.ts`، `PageHeader`، و`Placeholder` للشاشات غير المبنية بعد.
كل الأبعاد بخصائص منطقية (`start`/`end`، `ms`/`me`، `border-e`) فيعمل RTL/LTR تلقائيًا، ويستجيب حتى 400px. المالك: Dev B.

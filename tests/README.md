# tests/

اختبارات الوحدة بـvitest، بنفس هيكل `src/` و`shared/` (المنطق النقي فقط: الهاتف، متأخر، الانتقالات، ar/en).
التشغيل: `npm test` (يعمل في CI بعد lint). أول اختبارات: `tests/shared/` لمطابقة الـEnums واكتمال الترجمة (FOS-001).

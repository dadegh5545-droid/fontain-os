# .github/

إعدادات GitHub: `workflows/ci.yml` (typecheck، lint، format:check، build على Node 20)، `pull_request_template.md`، و`CODEOWNERS`.
`main` محمي: PR إجباري + مراجع واحد + CI أخضر + Squash merge (D-50).
